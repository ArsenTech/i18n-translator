import WindowWrapper from "@/components/window";

export default function SettingsPage(){
     return (
          <WindowWrapper hideMenubar title="Settings" hideMaximize>
               TODO: Apply Settings Window
          </WindowWrapper>
     )
}